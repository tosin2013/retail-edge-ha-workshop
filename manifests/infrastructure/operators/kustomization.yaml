apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
  - hyperconverged.yaml
  - eso-subscription.yaml
  - eso-rbac.yaml
  # eso-cluster-secret-store.yaml is managed by AgnosticD workload.yml
  # (applied after ArgoCD sync so ESO CRDs are guaranteed to exist)

commonLabels:
  app.kubernetes.io/part-of: retail-edge-ha-workshop
  app.kubernetes.io/component: operators
