apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RBAC resources will be added here when implemented
# Examples: ClusterRoles, RoleBindings, ServiceAccounts for students
resources: []

commonLabels:
  app.kubernetes.io/part-of: retail-edge-ha-workshop
  app.kubernetes.io/component: rbac
