apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RBAC resources for student access to workshop components and RHACM
resources:
  - rhacm-student-access.yaml

commonLabels:
  app.kubernetes.io/part-of: retail-edge-ha-workshop
  app.kubernetes.io/component: rbac
