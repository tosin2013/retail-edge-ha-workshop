= Module 0: Fleet Management Overview

== Introduction

Before diving into individual node HA configurations, let's understand how retail edge deployments are managed at scale.

In a real retail deployment:

* **100-1000+ stores** each with 2-4 edge devices
* **Centralized management** via Red Hat Advanced Cluster Management (RHACM)
* **Image-based updates** pushed to entire fleet
* **Compliance monitoring** across all locations
* **Remote troubleshooting** without on-site visits

== The Big Picture: Retail Edge at Scale

[ditaa, align=center]
....
    Fleet Distribution Across United States

 West Coast     Central Region    East Coast
 (150 stores)   (200 stores)      (150 stores)

    cBLU           cGRE              cRED
  +---------+    +---------+      +---------+
  |  o o o  |    | o o o o |      |  o o o  |
  | o o o o |    | o o o o |      | o o o o |
  | o o o o |    | o o o o |      | o o o o |
  |  o o o  |    | o o o o |      |  o o o  |
  +---------+    +---------+      +---------+
      |              |                 |
      +------+-------+--------+--------+
             |                |
         Regional         Regional
       Data Center      Data Center
         (West)           (East)
....

NOTE: Each 'o' represents approximately 10 retail stores. Total: 500 stores + 3 regional data centers.

=== Scenario: National Retail Chain

Imagine you're responsible for IT infrastructure at **RetailCorp**, a national chain with:

* **500 stores** across the United States
* **2 edge servers per store** (1,000 total devices)
* **3 regional data centers** providing backup services
* **Zero tolerance** for downtime during business hours (7 AM - 10 PM)

Each store runs critical applications:

* **Point-of-Sale (POS)** systems (process $10K-100K in transactions daily)
* **Inventory management** (real-time stock tracking)
* **Video analytics** (customer traffic, loss prevention)
* **WiFi authentication** (guest and employee networks)
* **Local databases** (transaction logs, product catalogs)

*Your challenge:* How do you ensure these 500 stores stay online when hardware fails?

== Why This Workshop Exists

The three HA architectures you'll learn map directly to real retail use cases:

[cols="1,2,2"]
|===
|Architecture |Best For |Typical Use Case

|**Pacemaker (Module 1)**
|Stateful applications with shared storage
|POS database servers, file servers

|**MicroShift + VRRP (Module 2)**
|Edge gateways and network services
|Network gateway, WiFi controller, video aggregator

|**Two-Node OpenShift (Module 3)**
|Regional clusters serving multiple stores
|Kubernetes platform for 10-50 stores (shared arbiter)
|===

== Red Hat Advanced Cluster Management (RHACM)

In production, you would manage this fleet using **RHACM** (formerly Advanced Cluster Management for Kubernetes).

=== Hub-Spoke Architecture

image::rhacm-hub-spoke.png[RHACM Hub-Spoke Architecture, align=center]

Each store is a **spoke cluster** managed from the central **hub cluster**.

=== Key Capabilities

**1. Inventory Management**

View all managed clusters across your fleet:

[source,yaml]
----
Name: store-0001-chicago-il
Status: Ready
OpenShift Version: 4.21.6
Nodes: 2 (worker + control-plane)
Workloads: 45 pods
Last Heartbeat: 30 seconds ago
----

**2. Policy-Based Governance**

Define compliance policies that apply to all stores:

[source,yaml]
----
apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: enforce-resource-limits
spec:
  disabled: false
  policy-templates:
  - objectDefinition:
      apiVersion: policy.open-cluster-management.io/v1
      kind: ConfigurationPolicy
      metadata:
        name: require-limits
      spec:
        remediationAction: enforce
        severity: high
        object-templates:
        - complianceType: musthave
          objectDefinition:
            apiVersion: v1
            kind: LimitRange
            metadata:
              name: resource-limits
----

If a store cluster drifts out of compliance, RHACM can automatically remediate.

**3. Application Deployment**

Deploy applications to specific store groups using **Placement**:

[source,yaml]
----
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: west-coast-stores
spec:
  predicates:
  - requiredClusterSelector:
      labelSelector:
        matchExpressions:
        - key: region
          operator: In
          values: ["west"]
        - key: environment
          operator: In
          values: ["production"]
----

Then deploy applications only to matching clusters:

[source,yaml]
----
apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: pos-application
spec:
  channel: store-apps/stable
  placement:
    placementRef:
      name: west-coast-stores
----

**4. Observability**

Aggregate metrics from all stores into a central dashboard:

* **Cluster health**: 498 healthy, 2 degraded
* **Resource utilization**: 65% CPU, 70% memory across fleet
* **Application status**: 99.8% pods running
* **Compliance**: 95% compliant (25 stores need patches)

=== Fleet Dashboard (Simulated)

NOTE: This workshop uses simulated dashboards. In production, you'd access the real RHACM console at `https://multicloud-console.apps.<hub-cluster>/`.

.RHACM Inventory Dashboard
[cols="1,1,1,1,1,1", options="header"]
|===
| Cluster Name | Status | Version | Nodes | Region | Last Seen

| store-0001-chicago-il
| ✅ Ready
| 4.21.6
| 2
| central
| 30s ago

| store-0002-boston-ma
| ✅ Ready
| 4.21.6
| 2
| east
| 45s ago

| store-0123-seattle-wa
| ✅ Ready
| 4.21.6
| 2
| west
| 1m ago

| store-0234-dallas-tx
| ❌ Offline
| 4.21.2
| 2
| central
| 5m ago

| store-0456-miami-fl
| ⚠️ Degraded
| 4.21.6
| 2
| east
| 2m ago

| ... (495 more stores)
| —
| —
| —
| —
| —
|===

**Summary Statistics:**

* Total Clusters: **1,003** (1,000 devices + 3 regional hubs)
* Status: **953 Ready**, **45 Offline**, **5 Degraded**
* Compliance: **95%** (980/1,003 compliant)

**Inventory View** shows:

* **1,000 devices** across 500 retail stores
* **95% online** (50 offline due to maintenance window)
* **Device groups**:
  ** POS-Servers (500 clusters)
  ** Edge-Gateways (500 clusters)
  ** Regional-Hubs (3 clusters)

Your workshop VMs would appear here as:

* `retail-edge-student-01-rhel-ha` (Group: Pacemaker-HA-Nodes)
* `retail-edge-student-01-microshift` (Group: MicroShift-Gateways)
* `retail-edge-student-01-twonode` (Group: OpenShift-Two-Node)

== Compliance and Security

image::fleet-dashboard-compliance.png[Fleet Compliance Status, align=center]

**Compliance Dashboard** shows:

* ✅ **980 clusters compliant** (97.7%)
* ⚠️ **15 clusters need security updates** (1.5%)
* ❌ **8 clusters out of compliance** (0.8%)

.Policy Violations by Type
[cols="3,1,1", options="header"]
|===
| Policy Name | Violated Clusters | Severity

| require-minimum-rhcos-version
| 5
| 🔴 High

| enforce-resource-limits
| 10
| 🟡 Medium

| network-policy-required
| 5
| 🟡 Medium

| pod-security-standards
| 3
| 🟢 Low
|===

Example policy violations:

[source,text]
----
Policy: require-minimum-rhcos-version
Failed Clusters:
  - store-0234-dallas-tx (RHCOS 4.21.2, required: 4.21.6)
  - store-0451-miami-fl (RHCOS 4.21.3, required: 4.21.6)

Remediation: Automated update scheduled for next maintenance window
----

== Update Orchestration at Scale

[mermaid]
....
gantt
    title Progressive Rollout - RHCOS 4.21.6 Update
    dateFormat YYYY-MM-DD
    section Canary
    Stage 1 (5% - 25 clusters)     :done, s1, 2026-03-25, 1d
    section Progressive
    Stage 2 (10% - 50 clusters)    :active, s2, 2026-03-26, 1d
    Stage 3 (25% - 125 clusters)   :s3, 2026-03-27, 1d
    Stage 4 (50% - 250 clusters)   :s4, 2026-03-28, 2d
    section Full Rollout
    Stage 5 (100% - 500 clusters)  :s5, 2026-03-30, 2d
....

**Update Status Summary:**

[cols="2,1,1,3", options="header"]
|===
| Stage | Clusters | Status | Progress

| Stage 1 (Canary)
| 25 (5%)
| ✅ Complete
| 25/25 updated successfully

| Stage 2
| 50 (10%)
| ⟳ In Progress
| 35/50 updated (70%)

| Stage 3
| 125 (25%)
| ⏳ Pending
| Scheduled for 2026-03-27 02:00 EST

| Stage 4
| 250 (50%)
| ⏳ Pending
| Scheduled for 2026-03-28 02:00 EST

| Stage 5
| 500 (100%)
| ⏳ Pending
| Scheduled for 2026-03-30 02:00 EST
|===

**Update Workflow:**

1. **Build new RHCOS image** with security patches
2. **Test on dev/staging clusters** (10 stores)
3. **Canary rollout** to 5% of stores (25 clusters)
4. **Monitor for 24 hours** (check for regressions)
5. **Progressive rollout**: 10%, 25%, 50%, 100%
6. **Automatic rollback** if failures detected

Example canary deployment:

[source,yaml]
----
apiVersion: ran.openshift.io/v1
kind: ClusterGroupUpgrade
metadata:
  name: rhcos-4.21.6-canary
spec:
  clusters:
  - store-0001-chicago-il
  - store-0123-boston-ma
  - store-0456-seattle-wa
  enable: true
  managedPolicies:
  - update-rhcos-4.21.6
  remediationStrategy:
    maxConcurrency: 1
    timeout: 240
----

**Maintenance Windows** ensure updates happen during off-peak hours:

* **US East Coast**: 2 AM - 5 AM EST
* **US West Coast**: 2 AM - 5 AM PST
* **Always avoid**: 7 AM - 10 PM (business hours)

== Remote Troubleshooting

When a store reports issues, you can troubleshoot remotely via RHACM:

**1. Check cluster status:**

[source,bash]
----
oc get managedcluster store-0234-dallas-tx -o yaml
----

**2. Access cluster directly:**

[source,bash]
----
oc config use-context store-0234-dallas-tx
oc get nodes
oc get pods -A
----

**3. Run diagnostics:**

[source,bash]
----
oc adm must-gather --dest-dir=/tmp/store-0234
----

**4. SSH to edge server (if needed):**

[source,bash]
----
# Via jumphost
ssh -J jumphost.datacenter.example.com core@store-0234-node1
----

All without sending a technician on-site!

== Your Workshop Environment

image::workshop-fleet-context.png[Workshop vs Production Fleet Scale, align=center]

While you'll work hands-on with **2-9 VMs per module**, imagine these represent a much larger fleet:

[cols="1,3,3"]
|===
|Your Lab |Scale Factor |Real-World Equivalent

|**2 VMs (Module 1)**
|200 stores × 2 nodes
|**400 Pacemaker servers** for POS databases

|**2 VMs (Module 2)**
|500 stores × 2 gateways
|**1,000 MicroShift edge gateways**

|**3 VMs (Module 3)**
|100 stores × 2 nodes + shared arbiter
|**200 OpenShift control-plane nodes + arbiters**
|===

=== What You'll Learn Applies to Fleets

Every command you run in this workshop applies to fleet management:

**Lab command:**
[source,bash]
----
virtctl start rhel-ha-node1 -n retail-edge-student-01
----

**Fleet equivalent (RHACM):**
[source,yaml]
----
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: PlacementDecision
metadata:
  name: start-all-pacemaker-nodes
spec:
  decisions:
  - clusterName: store-0001-chicago-il
  - clusterName: store-0002-boston-ma
  ... (498 more stores)
----

The HA patterns you learn (Pacemaker, VRRP, etcd quorum) work identically whether you're managing:

* **1 store** (your lab)
* **500 stores** (typical retail chain)
* **5,000 stores** (global enterprise)

== From Learning to Production

=== Your Workshop Journey

[source,text]
----
┌─────────────────────────────────────────────────────┐
│ Module 0: Fleet Management Overview (YOU ARE HERE)  │
│   ↓ Understand the big picture                      │
├─────────────────────────────────────────────────────┤
│ Module 1: Pacemaker HA                              │
│   ↓ Learn STONITH fencing + shared storage          │
├─────────────────────────────────────────────────────┤
│ Module 2: MicroShift with VRRP                      │
│   ↓ Learn virtual IP failover + local storage       │
├─────────────────────────────────────────────────────┤
│ Module 3: Two-Node OpenShift                        │
│   ↓ Learn etcd quorum + distributed architecture    │
├─────────────────────────────────────────────────────┤
│ Module 4: Chaos Engineering                         │
│   ↓ Validate resilience at scale                    │
└─────────────────────────────────────────────────────┘
----

=== Production Deployment Path

After this workshop, deploying to production involves:

**1. Design Phase (1-2 weeks)**

* Choose HA architecture per use case
* Size hardware (CPU, memory, storage)
* Plan network topology (Layer 2 requirements)
* Define SLAs and RTO/RPO targets

**2. Pilot Deployment (2-4 weeks)**

* Deploy to 5-10 pilot stores
* Validate functionality and performance
* Train on-site staff
* Document procedures

**3. Rollout (3-6 months)**

* Progressive deployment: 10 stores/week
* Monitor metrics continuously
* Iterate based on feedback
* Build runbooks for common issues

**4. Steady State Operations**

* RHACM for centralized management
* Automated compliance checking
* Monthly patching cycles
* Quarterly hardware refresh

== Real-World Success Stories

=== Case Study 1: Grocery Chain

**Customer:** 800-store grocery chain

**Challenge:** POS systems on standalone servers (no HA)

**Solution:** Deployed Pacemaker clusters (Module 1 architecture)

**Results:**

* **99.95% uptime** (was 97% before)
* **$2M saved** in lost transaction recovery
* **60-second failover** (was 4-hour manual recovery)

=== Case Study 2: Fast Food Franchise

**Customer:** 1,200 franchise locations

**Challenge:** WiFi controllers were single points of failure

**Solution:** Deployed MicroShift + VRRP (Module 2 architecture)

**Results:**

* **Zero guest WiFi outages** in 6 months
* **3-second failover** (guests don't notice)
* **50% cost reduction** vs proprietary WiFi controllers

=== Case Study 3: Regional Retail Cooperative

**Customer:** 150 stores sharing IT infrastructure

**Challenge:** Each store had 3-node OpenShift (expensive)

**Solution:** Two-node OpenShift + shared arbiter (Module 3 architecture)

**Results:**

* **40% cost savings** (2 servers/store vs 3)
* **Same workload capacity**
* **Shared arbiter** serves all 150 stores from regional datacenter

== Key Concepts to Remember

As you work through the modules, keep these fleet management principles in mind:

[cols="1,3"]
|===
|Principle |Why It Matters

|**Infrastructure as Code**
|All configurations in Git → easy to replicate across 1000s of stores

|**Observability**
|Can't fix what you can't see → centralized metrics/logs critical

|**Automation**
|Manual changes don't scale → automate updates, remediation, recovery

|**Standardization**
|Every store identical → easier troubleshooting, bulk operations

|**Defense in Depth**
|HA + backups + DR → multiple layers prevent total outages
|===

== Hands-On Lab: Exploring Your Fleet Manager

In this workshop, you have access to a **real RHACM hub cluster** managing your edge infrastructure. Let's explore the fleet management capabilities!

=== Lab Setup

Your workshop environment includes:

* **RHACM 2.16 Hub Cluster** (already deployed)
* **6 ManagedClusters** representing your edge sites (3 per student)
* **Fleet Manager Console** integrated into OpenShift

Each student manages a simulated retail store with 3 edge deployments:

[cols="1,2,2"]
|===
|ManagedCluster Name |Architecture |Purpose

|`student-{STUDENT_ID}-pacemaker`
|Pacemaker HA
|Represents your store's database servers (Module 1)

|`student-{STUDENT_ID}-microshift`
|MicroShift + VRRP
|Represents your store's edge gateway (Module 2)

|`student-{STUDENT_ID}-twonode`
|Two-Node OpenShift
|Represents your regional cluster (Module 3)
|===

=== Exercise 1: Access the Fleet Manager Console

The RHACM console is integrated into the OpenShift web console.

**Step 1:** Access the OpenShift Console

Open your terminal and get the console URL:

[source,bash,role=execute]
----
echo $RHACM_CONSOLE_URL
----

Or use the direct link:

[source,bash,role=execute]
----
oc whoami --show-console
----

**Step 2:** Login to OpenShift Console

. Open the console URL in a new browser tab
. Login with your workshop credentials (provided by instructor)
. Accept any security warnings if prompted

**Step 3:** Navigate to Fleet Manager

. Click **All Clusters** in the top navigation bar (or use the cluster switcher)
. Go to **Infrastructure** → **Clusters** in the left navigation

You should see the RHACM dashboard with your managed clusters!

=== Exercise 2: View Your Fleet Inventory

Now let's explore your edge fleet using the CLI.

**Step 1:** List All Managed Clusters

[source,bash,role=execute]
----
oc get managedclusters
----

You should see:

* `local-cluster` - The hub cluster itself
* Your 3 edge sites (pacemaker, microshift, twonode)
* Other students' edge sites (if in a multi-user session)

**Step 2:** View Your Fleet with Details

[source,bash,role=execute]
----
oc get managedclusters -l student-id=$STUDENT_ID -o custom-columns=\
NAME:.metadata.name,\
ARCHITECTURE:.metadata.labels.architecture,\
LOCATION:.metadata.labels.location,\
MODULE:.metadata.labels.module
----

This shows your 3 edge sites organized by:

* **Architecture** - The HA pattern (pacemaker-ha, microshift-vrrp, two-node-openshift)
* **Location** - Your simulated store location (edge-store-00X)
* **Module** - Which workshop module covers this architecture

**Step 3:** Filter Fleet by Architecture

See all Pacemaker HA deployments across the fleet:

[source,bash,role=execute]
----
oc get managedclusters -l architecture=pacemaker-ha
----

See all MicroShift deployments:

[source,bash,role=execute]
----
oc get managedclusters -l architecture=microshift-vrrp
----

💡 **Real-World Application:** In production, you'd have labels like `region=west`, `store-type=flagship`, `compliance-level=pci` to organize hundreds of stores.

=== Exercise 3: Inspect a Managed Cluster

Let's dive deeper into one of your edge sites.

**Step 1:** Get Detailed Information

[source,bash,role=execute]
----
oc describe managedcluster student-$STUDENT_ID-pacemaker
----

Look for:

* **Labels** - How this cluster is categorized
* **Status** - Whether the cluster is accepted and joined
* **Conditions** - Health indicators

**Step 2:** View Cluster Labels

[source,bash,role=execute]
----
oc get managedcluster student-$STUDENT_ID-pacemaker -o jsonpath='{.metadata.labels}' | jq '.'
----

These labels enable:

* **Placement decisions** - Deploy apps only to specific stores
* **Policy targeting** - Apply governance rules to groups
* **Search and filtering** - Find clusters by attributes

=== Exercise 4: Understand Fleet Organization

In a real retail deployment, you'd organize stores by business attributes.

**Step 1:** Count Clusters by Architecture

[source,bash,role=execute]
----
echo "Pacemaker HA stores: $(oc get managedclusters -l architecture=pacemaker-ha --no-headers | wc -l)"
echo "MicroShift stores: $(oc get managedclusters -l architecture=microshift-vrrp --no-headers | wc -l)"
echo "Two-Node OpenShift: $(oc get managedclusters -l architecture=two-node-openshift --no-headers | wc -l)"
----

**Step 2:** View Fleet Summary

[source,bash,role=execute]
----
cat <<'EOF'
Fleet Distribution
==================
EOF

oc get managedclusters -l location=edge-store-001 --no-headers | wc -l | xargs -I {} echo "Store 001: {} edge systems"
oc get managedclusters -l location=edge-store-002 --no-headers | wc -l | xargs -I {} echo "Store 002: {} edge systems"
----

**Real-World Scale:**

* Lab: 6 edge systems (2 stores × 3 systems)
* Production: 1,000-3,000 edge systems (500 stores × 2-6 systems)

=== Exercise 5: Fleet Manager Console Exploration (Optional)

If you have access to the OpenShift web console, explore these features:

**In the RHACM Console:**

1. **Clusters View**
   * See all managed clusters at a glance
   * Filter by labels (try filtering by your student ID)
   * Check cluster status and health

2. **Search Feature**
   * Search for resources across all clusters
   * Try searching: `kind:ManagedCluster label:student-id={STUDENT_ID}`

3. **Overview Dashboard**
   * View fleet-wide statistics
   * See cluster distribution
   * Monitor compliance status

NOTE: The console provides a visual interface for the same data you just explored via CLI. In production, operations teams use the console for day-to-day fleet management, while automation uses the CLI/API.

=== What You Learned

In this hands-on lab, you:

✅ Accessed a real RHACM hub cluster
✅ Viewed your managed edge clusters (fleet inventory)
✅ Filtered and organized clusters using labels
✅ Explored fleet organization patterns
✅ Understood how lab VMs map to production scale

**Key Takeaway:** The 3 ManagedClusters you see represent the 3 HA architectures you'll build in Modules 1-3. In production, these same patterns scale to hundreds or thousands of stores.

== Next Steps

Now that you understand the **big picture** of retail edge fleet management, it's time to get hands-on!

In the next module, you'll:

1. **Start VirtualMachines** representing edge servers
2. **Configure Pacemaker** with STONITH fencing
3. **Test failover** scenarios
4. **Add shared storage** with GFS2

Remember: The 2 VMs you configure represent **hundreds or thousands** of similar deployments in a real retail fleet. The skills you learn here apply at any scale!

== Workshop Architecture Reminder

image::module0-workshop-vms.png[Workshop VM Layout, align=center]

NOTE: Each set of VMs is isolated via User Defined Networks (UDNs) providing Layer 2 connectivity for multicast protocols.

== Ready to Begin?

You now understand:

* ✅ How retail edge deployments work at scale (100-1000s of stores)
* ✅ Red Hat Advanced Cluster Management for fleet management
* ✅ Why you're learning three different HA architectures
* ✅ How your lab VMs relate to real-world production

Click **Next Module** below to start with **Module 1: Pacemaker HA Cluster**.

Let's build something resilient! 🚀
